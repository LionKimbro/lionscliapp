[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lionscliapp"
version = "0.1.0"
description = "A humane CLI application framework"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
